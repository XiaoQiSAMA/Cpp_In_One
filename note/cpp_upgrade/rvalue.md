# 右值引用

```c++
int a = 10;
int &b = a; // 左值：有内存，有名字  
// int &&c = a;    // 无法将左值绑定到右值引用
// int &c = 20;    // 不能用左值引用绑定一个右值
/*
int tmp = 20;
const int &c = 20
*/
const int &c = 20;

// 右值：没名字(临时量)，没内存
int &&d = 20;   // 右值引用绑定右值

int &&f = d;    // 一个右值引用变量，本身是一个左值
```
